<%- include('partials/header') %>
<!--Try to run css (might be because the cssstylesheet.ejs could not be accessed in the same way with the gallery.ejs and index.ejs-->
<link rel='stylesheet' href='/css/style.css'>
<!---->
    <div id="main">
        <div id="site_content">
            <div class="item">
                        <figure>
                            <img src="/images/lrg/<%= gallery.fileName %>" alt="<% gallery.title %>" >
                            <h3> <%= gallery.title %> </h3>
                            <figcaption><a class="source" href="<%= gallery.attribution.url %>"> <%= gallery.attribution.credit %> </a> / from <%= gallery.attribution.source %> </figcaption>
                        </figure>
            </div> 
        </div>
    </div>  


<%- include('partials/footer') %>